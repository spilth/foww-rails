<%= form_with(model: force_unit) do |form| %>
  <% if force_unit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(force_unit.errors.count, "error") %> prohibited this force_unit from being saved:</h2>

      <ul>
        <% force_unit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :force_id, class: "form-label" %>
    <%= form.collection_select(:force_id, Force.all.order(:name), :id, :name, {}, {class: "form-select"}) %>
  </div>

  <div>
    <%= form.label :unit_id, class: "form-label" %>
    <%= form.collection_select(:unit_id, Unit.all.order(:name), :id, :name, {}, {class: "form-select"}) %>
  </div>

  <div>
    <%= form.label :cost, class: "form-label" %>
    <%= form.number_field :cost, class: "form-control" %>
  </div>

  <%= form.submit class: "btn btn-primary" %>
<% end %>

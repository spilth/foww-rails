<%= form_with(model: force, class: "row g-3") do |form| %>
  <% if force.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(force.errors.count, "error") %> prohibited this force from being saved:</h2>

      <ul>
        <% force.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-4">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="col-md-4">
    <%= form.label :description, class: "form-label" %>
    <%= form.text_area :description, class: "form-control" %>
  </div>

  <%= form.submit class: "btn btn-primary" %>
<% end %>
